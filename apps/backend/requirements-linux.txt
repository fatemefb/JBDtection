# ═══════════════════════════════════════════════════════════════════════
# JBDetection — Production Requirements (PaddleOCR-based, no Tesseract)
# ═══════════════════════════════════════════════════════════════════════
#
# This file replaces the legacy requirements-linux.txt that had
# Tesseract + TensorFlow + PyTorch dependencies. Those are removed:
#
#   ✗ pytesseract        — removed (we use PaddleOCR)
#   ✗ tensorflow-gpu     — removed (was only for Keras PDF classifier,
#                           which is now optional and uses native
#                           digital/scanned auto-detection)
#   ✗ keras              — removed (same reason)
#   ✗ torch/torchvision  — removed (unused)
#   ✗ opencv-python-headless — removed (opencv-python is enough)
#   ✗ RapidFuzz          — removed (we use python-Levenshtein only)
#
# Install order matters! PaddlePaddle MUST be installed before PaddleOCR:
#
#     pip install -r requirements-linux.txt
#
# For GPU support, replace `paddlepaddle` with `paddlepaddle-gpu`:
#
#     pip install paddlepaddle-gpu==2.6.2
#     pip install -r requirements-linux.txt --no-deps paddlepaddle
#
# Tested with Python 3.12 + the versions below. All conflicts resolved.
# ═══════════════════════════════════════════════════════════════════════

# ── OCR Engine (install paddlepaddle BEFORE paddleocr) ──────────────
paddlepaddle==2.6.2
paddleocr==2.10.0

# ── Image / PDF Processing ───────────────────────────────────────────
opencv-python==4.10.0.84
Pillow==10.4.0
PyMuPDF==1.26.7
pdf2image==1.17.0

# ── Data Processing ──────────────────────────────────────────────────
numpy<2.0
pandas==2.2.3
openpyxl==3.1.5

# ── String Matching (for tag fuzzy matching) ────────────────────────
python-Levenshtein==0.25.1

# ── PDF Report Generation ──────────────────────────────────────────
reportlab==4.4.9

# ── Web Framework ───────────────────────────────────────────────────
Flask==3.0.3
Flask-Cors==4.0.1
Werkzeug==3.0.6
gunicorn==23.0.0
Jinja2==3.1.6
itsdangerous==2.2.0
click==8.1.8
blinker==1.8.2
MarkupSafe==2.1.5
pydantic==2.8.2

# ── Async Task Scheduling ────────────────────────────────────────────
APScheduler==3.10.4

# ── Database ─────────────────────────────────────────────────────────
SQLAlchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.9
python-dotenv==1.0.1

# ── System / Logging / Utils ────────────────────────────────────────
psutil==7.0.0
colorama==0.4.6
rich==14.0.0
Pygments==2.19.2

# ── Date / Time ─────────────────────────────────────────────────────
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.2

# ── Networking ──────────────────────────────────────────────────────
requests==2.32.4
certifi==2025.7.14
charset-normalizer==3.4.2
urllib3==2.2.3
idna==3.10

# ── File Handling ───────────────────────────────────────────────────
filelock==3.16.1
fsspec==2023.6.0
et_xmlfile==2.0.0

# ── Build tool (paddlepaddle 2.6.2 requires setuptools<81) ────────
setuptools<81

# ═══════════════════════════════════════════════════════════════════════
# REMOVED packages (do NOT install these — they cause conflicts):
# ═══════════════════════════════════════════════════════════════════════
# pytesseract==0.3.13          # Replaced by PaddleOCR
# opencv-python-headless      # Conflicts with opencv-python
# RapidFuzz==3.9.7            # Conflicts with python-Levenshtein
# tensorflow-gpu==2.10.0      # Conflicts with paddlepaddle (both need protobuf<4)
# tensorflow-estimator==2.10.0
# tensorflow-io-gcs-filesystem==0.30.0
# tensorboard==2.10.1
# tensorboard-data-server==0.6.1
# tensorboard-plugin-wit==1.8.1
# keras==2.10.0               # Optional — only if using legacy PDF classifier
# Keras-Preprocessing==1.1.2
# torch==2.2.2                # Unused
# torchvision==0.17.2         # Unused
# torchaudio==2.2.2           # Unused
# absl-py==2.3.1              # TF dependency
# astunparse==1.6.3           # TF dependency
# flatbuffers==25.2.10        # TF dependency
# gast==0.4.0                 # TF dependency
# google-auth==2.40.3         # TF dependency
# google-auth-oauthlib==0.4.6 # TF dependency
# google-pasta==0.2.0         # TF dependency
# grpcio==1.70.0              # TF dependency
# h5py==3.11.0                # TF dependency (only needed if using keras_model.h5)
# libclang==18.1.1            # TF dependency
# Markdown==3.4.4             # TF dependency
# markdown-it-py==3.0.0       # TF dependency
# mdurl==0.1.2                # TF dependency
# ml_dtypes==0.2.0            # TF dependency
# namex==0.1.0                # TF dependency
# oauthlib==3.3.1             # TF dependency
# opt-einsum>=3.3.0           # TF dependency (kept loose if needed)
# protobuf==3.19.6           # Pin to <4 for paddlepaddle compatibility
# pyasn1==0.6.1              # TF dependency
# pyasn1_modules==0.4.2      # TF dependency
# rsa==4.9.1                 # TF dependency
# termcolor==2.4.0           # TF dependency
# wrapt==1.17.2              # TF dependency
# sympy==1.13.3              # Torch dependency
# mpmath==1.3.0              # Torch dependency
# networkx==3.1              # Torch dependency
# optree==0.15.0             # Torch dependency
# six==1.17.0                # Legacy compat
# typing_extensions==4.13.2  # Auto-installed by other packages
# packaging==25.0            # Auto-installed
# cachetools==5.5.2          # TF dependency
# requests-oauthlib==2.0.0   # TF dependency
